export default function Footer() {
  return (
    <footer className="bg-darkCard text-center py-4 text-sm text-gray-400">
      © {new Date().getFullYear()} STELLE CARD — Premium Mobility
    </footer>
  );
}
